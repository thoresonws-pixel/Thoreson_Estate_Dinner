const assert=require('assert');require('../cooperative-maze.js');const M=global.CooperativeMaze;const c={durationSeconds:40,shufflePauseSeconds:3,mazes:[{id:'violet_route',label:'Violet',symbol:'V',color:'#ffaaff',grid:['#####','#...#','###.#','#...#','#####'],start:[1,1],goal:[1,3]}]};M.validate(c);let s=M.start(c,['pilot','reader'],0,'space-test');for(const direction of M.route(c.mazes[0],[1,1]))s=M.move(s,c,'reader',{attempt:s.attempt,revision:s.revision,mazeId:'violet_route',direction},1000);assert(s.solved);assert.throws(()=>M.validate({...c,mazes:[{...c.mazes[0],goal:[0,0]}]}));console.log('PASS alternate story maze layout, identities, timings and invalid endpoints.');
